export type SortingEntity = {
    sortingBy: {
        name: {
            ascending: boolean,
            descending: boolean,
        },
        office: {
            ascending: boolean,
            descending: boolean,
        },
    }
}
